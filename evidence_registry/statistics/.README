# Statistical Evidence Artifacts

**Purpose:** Empirically prove the environment can sustain Bayesian inference computational load.

## Required Artifacts:

### Baseline Capture (Phase B)
- `python_env_baseline.txt` - Current Python packages (pip list/freeze) and import test results

### Scientific Stack Validation (Phase C)
- `stack_verification_log.txt` - stdout/stderr from verify_science_stack.py execution
- `model_results.json` - Structured JSON with R-hat, ESS, sampling metadata
- `trace_plot.png` - Visual convergence diagnostic (if generated)

## Evidence Standards:
- Document all scientific library versions
- Capture compilation status (no segfaults or linking errors)
- Store machine-readable convergence diagnostics
- Include sampling parameters (chains, samples, warmup)

## Exit Gate Requirements:
- PyMC, ArviZ, NumPy, Pandas install without compilation errors
- Import tests succeed
- Minimal model samples successfully
- R-hat < 1.01 (convergence proof)
- ESS > 100 (effective sample size proof)
- No segmentation faults or BLAS/LAPACK linking errors

