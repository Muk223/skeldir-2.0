---
description: "API endpoint patterns: attribution, ingestion, webhooks"
globs:
  - "backend/app/api/**"
  - "backend/app/webhooks/**"
priority: 90
model_preference: "claude-4.5-opus"
---

# Source of truth
Endpoint patterns (attribution, ingestion, webhooks) are defined in `AGENTS.md`
under Endpoint Patterns. Treat `AGENTS.md` as canonical; this file only routes
Cursor’s auto-attach to those rules.
