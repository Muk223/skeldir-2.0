# B2.5-P14 Corrective Action Remediation VII Report

**Scope**: `B2.5-P14 Corrective Action Remediation VII-3` (Agent-3 successor / continuation directive, §§0–40)
**Governing corpus**: audits 73 (Nicholas) / 74 (Trey), Agent-1 VII log, Agent-2 VII log, Corrective VII directive, Corrective VII-2
**Engineering disposition**: `CORRECTIVE VII = CANDIDATE COMPLETE` (product proposition evidenced below)
Phase completion (`B2.5-P14 = COMPLETE`) remains independent audit authority (VII-3 §33).

Operating model preserved throughout: `PRESERVE EXISTING BYTES → RAPIDLY RECONSTRUCT CURRENT STATE → CONFIRM LOAD-BEARING SUCCESSOR RESULTS → RESOLVE THE FIRST REMAINING RED → CONTINUE FORWARD`. No Agent-1/2 working subsystem was redesigned for preference; the one inherited mechanism refuted by current empirical evidence (VII final-source NULL semantics) was reopened with its causal dependents only (§38).

---

## 0. Coordinates

| Fact | Value | How resolved |
|---|---|---|
| Entering protected `main` | `12db04030155edeef835bf4f77eeb6174efdc784` | frozen audited SHA (audits 73/74); `git rev-parse HEAD` on entering branch |
| Entering tree | `9193f2285d8927ace80b28d8b8ad279148429510` | `git rev-parse 'HEAD^{tree}'` before changes |
| Entering Alembic head | `202609071200`, single head | `migration_graph_head()` on entering tree |
| Agent-3 branch | `codex/p14-corrective-vii2` | this branch (inherited Agent-2 branch name) |
| First-observed status | 11 modified, 10 untracked; HEAD `12db0403` | `git status --porcelain=v1` at session open |
| Candidate SHA | `51a6e28f78926f211d081c5c2231c03d5c523817` | `git rev-parse HEAD` after final fix commit |
| Candidate tree | `1464c72a5b205dac9f78997bfa86a681b0cd6132` | `git rev-parse 'HEAD^{tree}'` |
| Candidate Alembic head | `202609072001`, single linear head | `migration_graph_head()`; `202609072000 → 202609072001` |
| Pull request | `#722` | `gh pr create --base main` |
| Changed files | 35 (24 modified, 11 added) | `git diff --name-status origin/main...HEAD` |
| Landed `main` SHA | *(to be filled after merge queue)* | `git ls-remote origin refs/heads/main` after landing |
| Landed `main` tree | *(must equal candidate tree)* | `git rev-parse 'HEAD^{tree}'` on landed main |
| Merge governance | `main-merge-queue` / ALLGREEN, no bypass | PR merge-group evidence + branch protection census |

**Changed-file inventory**

```text
M .github/workflows/b2_5-p13-e2e-trust-closure.yml
A alembic/versions/007_skeldir_foundation/202609072000_b25_p14_r7_production_conduction.py
A alembic/versions/007_skeldir_foundation/202609072001_b25_p14_vii3_fail_closed.py
A api-contracts/openapi/v1/trust-simulations.yaml
M backend/app/api/health.py
A backend/app/api/trust_simulations.py
M backend/app/core/construction_authority.py
A backend/app/core/physical_authority.py
M backend/app/main.py
A backend/app/schemas/trust_simulations.py
M backend/app/trust/builder.py
A backend/app/trust/policy_configuration.py
M backend/tests/trust/test_b25_p13_c19_context_robust_topology.py
M backend/tests/trust/test_b25_p14_b28_simulation_conservation.py
M backend/tests/trust/test_b25_p14_gate0_issuance_consequence_authority.py
M backend/tests/trust/test_b25_p14_r4_downstream_consequence.py
M backend/tests/trust/test_b25_p14_r6_possession_authority.py
A backend/tests/trust/test_b25_p14_vii2_channel_authority.py
A backend/tests/trust/test_b25_p14_vii2_issuance_selection.py
M backend/tests/trust/test_b25_p5_builder.py
A contracts/trust-api/authority-schema.v1.json
A contracts/trust-api/production-conduction.v1.md
M contracts/trust-api/provenance-chain.schema.json
M contracts/trust-api/trust-api.openapi.yaml
M db/schema/canonical_schema.sql
M docs/ci/enforcer_registry.yaml
M docs/ci/gate_subsumption_matrix.yaml
A docs/forensics/B2.5-P14 Corrective Action Remediation VII Report.md
M docs/forensics/INDEX.md
A scripts/ci/regenerate_b25_p14_authority_manifest.py
M scripts/ci/validate_b25_p14_downstream_authority.py
M scripts/ci/validate_b25_p1_trust_drift.py
M scripts/ci/validate_b25_p2_canonicalization.py
M scripts/database/prepare_migration_authority_boundary.py
```

Line-ending discipline (§22): the inherited Agent-1/2 working tree carried CRLF in `*.py`/`*.json` working copies against a `text eol=lf` policy. Accidental CRLF was converted to LF (byte-evident: `*.py text eol=lf` in `.gitattributes`, HEAD blobs LF); no unrelated repository file was normalized and no semantic byte was altered for line endings. An early recovery error that truncated 12 working files to zero bytes is recorded in the first-red ledger; all 12 were restored byte-identical in size from the retained Agent-1 worktree (`skeldir-p14-vii-local-20260907`, branch `codex/p14-corrective-vii`) plus Agent-2's documented `trust_simulations.py` hardening, re-verified by `git diff --stat` sizes and the VII-2 unit battery (4/4) before proceeding.

---

## 1. Initial findings (what Agent-3 first observed)

1. **Successor bytes present.** The 11 modified + 10 untracked files matched the Agent-2 handoff (migration `202609072000`, final-identity view, policy events, wall-clock possession, physical manifest, production simulation/request/read surfaces, VII-2 proof files). Provisionally confirmed per §23 after recheck.
2. **VII-2 battery green.** `test_b25_p14_vii2_issuance_selection.py` + `test_b25_p14_vii2_channel_authority.py`: 11 passed on the inherited `p14_candidate` database (head `202609072000`).
3. **Four legacy reds reproduced exactly** on `p14_candidate` (PostgreSQL 15, port 55488), same four Agent-2 stopped at:
   - `test_p14_gate10_new_trust_supersedes_dependent_explanations` → `b27_explanation_requires_durable_issuance`
   - `test_p14_gate10_explanation_cache_identity_is_tenant_scoped_at_the_database` → same
   - `test_p14_gate7_the_database_refuses_a_non_conserving_allocation` → `b28_request_final_identity_mismatch`
   - `test_p14_gate9_the_database_refuses_an_escalated_action_authority` → same class (request refused before result guard)
4. **A load-bearing product defect behind the manifest.** Fresh replay (`skeldir_vii2_a`, port 55490) carried `IF source_id IS NOT NULL AND …` in `b28_enforce_final_source_identity()`, while the retained Agent-1 database (`p14_candidate`) carried `IF source_id IS NULL OR …`. With a permissive current policy, the migration-file semantics admit an unknown source (fail-open); the Agent-1 database fails closed. Agent-2's manifest regeneration had canonized the migration-file (fail-open) text. This refutes the inherited NULL handling and reopens it per §38.
5. **Stale timestamp comments on old databases.** `p14_candidate` retained pre-VII `physically_observed: …` column comments; fresh replays carry canonical `OBSERVED EVENT. …`. The R6 field-semantics taxonomy classifies only the latter, so old databases redden one R6 leg while fresh databases stay green — catalog drift, not a guard regression.

---

## 2. Remediations

### 2.1 Fail-closed final-source identity (product fix, §4.4/§13/H-VII3-04)

New linear migration `202609072001_b25_p14_vii3_fail_closed.py` (`202609072000 → 202609072001`):

```sql
IF source_id IS NULL THEN
  RAISE EXCEPTION 'b28_request_requires_durable_issuance:%', NEW.source_issuance_envelope_hash;
END IF;
IF source_id IS DISTINCT FROM NEW.source_envelope_id THEN
  RAISE EXCEPTION 'b28_request_final_identity_mismatch';
END IF;
```

NULL now refuses with the same durable-issuance error the downstream request-consequence guard raises, so VII-2 selector proofs adjudicate one law at either layer (unknown hash, wrong envelope/hash pair, cross-tenant hash, zero rows → REFUSE; no `ORDER BY … LIMIT 1` fallback exists anywhere in the path). The same migration re-asserts the four canonical wall-clock column comments so `empty→head` and `predecessor→head` converge. `REQUIRED_SCHEMA_REVISION` advanced to `202609072001`; the migration-graph head test constrains it.

Falsifier: dropping `trg_b28_final_source_identity` on a disposable correctly-stamped candidate turns `assert_physical_authority` RED (`database_physical_authority_mismatch`); exact restoration restores GREEN. The unknown-source leg was additionally verified to refuse before any policy evaluation.

### 2.2 Legacy reconciliation (H-VII3-01 confirmed; production fixed nothing, fixtures updated)

Disposition for each of the four: **old fixture assumption invalid under the new production ontology; target invariant intact; fixture reconstructed with minimum lawful production-shaped prerequisites; RED mutation still fails for the target reason.**

- **Gate-10 pair (B2.7).** Old helpers inserted `trust_envelope_issuance_log` rows with no signer lineage, so no `trust_final_issuance_identity` row exists and the B2.7 consequence guard (`IF NOT FOUND → b27_explanation_requires_durable_issuance`) refuses before supersession/cache logic runs. `_seed_terminal_issuance` now persists full lineage (ledger authorized → signing → signature_known → issued via the lawful issuer/signer roles; `signed_envelope` JSON carrying `envelope_id`/`semantic_truth_hash`/`policy_state`; terminal issuance row) and publishes the matching current governed policy. `_insert_materialization` binds `source_envelope_id` to the issuance's exact identity. Supersession (T1 stale on T2 for same subject, same-truth no-stale) and tenant-scoped cache identity (cross-tenant share allowed, intra-tenant duplicate refused) now adjudicate past the new guards. 2/2 green.
- **Gate-7 (allocation conservation).** Old `_insert_request` invented a random `source_envelope_id` unrelated to any issuance, so the VII final-source trigger refuses before the result-conservation trigger is reached. The helper now uses the issuance's exact `source_envelope_id`; the short-allocation and fractional-money legs again reach the conservation guard (`conserve the requested budget` / `fractional money`) with the consequence trigger severed and restored as the test prescribes. Green.
- **Gate-9 (action-authority escalation).** Same early-refusal root cause; with exact identity + `approval_required` current policy published, layer 1 (derivation guard `b28_result_action_authority_not_derived`) and layer 2 (CHECK alone with the trigger severed) both refuse `approval_required` as the test requires. Green.

No production guard was weakened. The R4 severance test that proves the request-consequence guard + FK load-bearing was updated to sever the third VII layer (`trg_b28_final_source_identity`) alongside them for the measurement and to verify all three restorations byte-identical — old assumption (two layers) recorded, new prerequisite (three layers) explicit, target invariant unchanged. The B28 scheduler-reachability test allowlists the single governed production importer (`backend/app/api/trust_simulations.py`, the supported request-driven HTTP boundary) while still forbidding every `tasks/`/`workers/`/scheduler reach — old assumption (zero importers) recorded, new prerequisite (one request-driven importer) explicit.

### 2.3 API hardening preserved (Agent-2, re-verified)

- `derive_simulation_channels(rows)`: pure evidence boundary — one currency → integer-minor-unit channel evidence; mixed/missing → HTTP 422. Unit-proved (4/4) without a topology.
- `_presented_token(request)`: valid Bearer → credential re-presented to the possession authority; missing/malformed/wrong scheme → HTTP 401. Unit-proved.
- `_source`: exact `(tenant, source_envelope_id, final_envelope_hash)` selector, single-row requirement (404 otherwise), final-hash recomputation + semantic-truth equality + signature verification (409), issuance-time policy admissibility (403), expiry (409). No `envelope_id`-only fallback.
- `_evidence`: server-side snapshot derivation, Trust-snapshot equality (409 on drift — evidence cannot silently move from snapshot A to B while citing A), then the pure money law above. Caller supplies intent/budget/selector/credential only; never revenue, channels, tenant, or policy.

### 2.4 Physical authority manifest + migration equivalence (§20–§21)

- `scripts/ci/regenerate_b25_p14_authority_manifest.py` retained: manifest is generated from an independently migrated candidate database, never hand-edited, never learned from a serving DB.
- Regenerated at `202609072001`: 379 objects, revision `202609072001`, `assert_physical_authority(fresh DB) → PASS`.
- Universe A (`empty → 202609072001`) vs Universe B (`202609071200 → … → 202609072001`): 379/379 objects, 0 missing, 0 extra, 0 differing definitions → `AB_EQUIVALENT`.
- Negative control (disposable correctly-stamped DB, drop `trg_b28_final_source_identity`): manifest check RED; exact restore GREEN.

### 2.5 CI proof-plane closure (§32)

Added merge-authoritative step `P14 Corrective VII-2 exact issuance and authority proofs` to `b2-5-p14-downstream-projection-safety`, asserting `11 passed` over the two VII-2 files. The step runs inside the required P14 job, which the `B2.5-P13 E2E Trust Closure` aggregate gates on (`P14_DOWNSTREAM_RESULT = success` asserted; aggregate required; strict + `enforce_admins`). A VII/VII-2 proof failure therefore fails the aggregate and blocks the merge queue. YAML validated.

---

## 3. Validation ledger (falsifiable)

| Proposition | Evidence | Result |
|---|---|---|
| 11 successor tests green | `test_b25_p14_vii2_issuance_selection.py` (7) + `test_b25_p14_vii2_channel_authority.py` (4) on `p14_candidate@202609072001` | 11 passed |
| 4 legacy reds reconciled for correct reasons | Gate-10 ×2, Gate-7, Gate-9 on same DB | 4 passed (were 4 failed) |
| Regression corpus green | Gate-0 (14) + R4-downstream (11) + R4-terminal (6) + R5-causal (12) + R5-construction (9) + R6 (12) + VII-2 (11) | 75 passed |
| Preservation (B2.7/B2.8/contracts) | B27 conservation (88) + B28 (34) + projection floor (19) + P5 builder + C20/C21 (16) | all green; validator `PASSED` |
| NULL fail-closed | unknown hash / wrong pair / cross-tenant / zero rows → refuse; permissive-policy + unknown source → `requires_durable` (not admitted) | proved on `p14_candidate` |
| Multi-issuance exact selection | shared `envelope_id`, distinct final hashes; A+hash(A)→A, B+hash(B)→B; cross pair refused; unknown refused; cross-tenant refused | VII-2 H01 legs green |
| Dual-conjunct policy | sim+sim→may admit; sim+read_only→refuse; read_only+sim→refuse (issuance leg); revocation sim→read_only refuses new requests with zero side effects | VII-2 H04 legs green |
| Publisher least-privilege + append-only | runtime roles cannot INSERT; admin cannot UPDATE/DELETE history (permission-denied then trigger `trust_policy_append_only` for owner); tenant-cascade delete excepted by design | VII-2 H10 legs green |
| Mapping derived, not writable | UPDATE/DELETE refused for app_user/requester/solver/issuer | VII-2 H09 green |
| Wall-clock freshness | two mints 1.5s apart in one txn differ ≥1.0s; 901s-aged witness → `witness_expired`; fresh witness admits | VII-2 temporal green |
| Money conservation | single currency preserved exactly as integers; mixed/missing → 422; no floats; no request-authored currency | channel battery 4/4 |
| Credential boundary | Bearer re-presentation; five malformed shapes → 401 | channel battery 4/4 |
| Duplicate/retry | same `request_ref` → refused; new ref + same source/budget → distinct lawful request `(2,2,2,2)` census | VII-2 H07 green |
| Cross-tenant request/read | cross-tenant selector → refused; read path tenant-bound (`WHERE tenant_id=:tenant`, 404 otherwise) | VII-2 + C19 legs |
| Snapshot drift | Trust-A evidence vs snapshot-B → 409 `request a fresh Trust` (code path + C19 topology) | preserved |
| B2.7 final-identity coherence | Gate-10 greens above use the same final Trust ontology as B2.8; supersession keyed on `(subject, semantic)`; cache identity tenant-scoped | green |
| B2.7 semantic conservation | causal/counterfactual/untrusted-label prose refused; novel-prose battery via conservation suite | 88 green |
| B2.4/B2.13 isolation | confidence never promoted to causal lift; deterministic simulation ≠ incrementality | conservation legs green |
| Zero external execution | validator `PASSED`; scheduler test green with single allowlisted request-driven importer; beat contains no B2.8 string | proved |
| Physical manifest | 379 objects @`202609072001`; fresh-DB assert PASS; A/B equivalent; drop-trigger falsifier RED→GREEN | proved |
| C19 production journey | Agent-1 full positive (real Bayesian fit → signed Trust → HTTP simulation → durable result/proposal → authorized read; cross-tenant read refused; read_only negative with zero side effects) inherited as experimental evidence; VII-3 changes are strictly stricter (fail-closed) or test-only, and the C19 P14 legs (`P14_VII_POSITIVE_READ_AND_NEGATIVE_CONSERVATION_PASS`) are preserved in-tree; required CI (which rebuilds production images and migrates to the candidate head) adjudicates the journey on the exact tree | inherited + CI-gated |

First-red ledger: (1) 12 working files truncated to zero bytes by a line-ending normalization one-liner that opened each file for writing before reading it — restored from the retained Agent-1 worktree plus the documented Agent-2 `trust_simulations.py` hardening, re-verified by size census and the 4/4 unit battery; (2) `p14_candidate`'s pre-VII timestamp comments reddening one R6 leg — converged via the new migration re-asserting canonical comments; (3) R4 severance expectation (two layers) vs three VII layers — updated to sever/restore all three with byte-identical restoration; (4) B28 scheduler test vs the new governed importer — allowlisted the single request-driven production route; (5) `app.schemas/trust_simulations.py` missing from the candidate (gitignored generated schema): CI collection failed with `ModuleNotFoundError` while local runs passed — force-added per the repo's generated-schema convention after verifying its datamodel-codegen header; (6) C16 issuer-grant contract vs the new final-identity view grant — updated the expected grant string; (7) canonical-schema drift (VII objects absent; then `--no-comments` flag mismatch vs the schema-authority gate) — regenerated from a fresh head database with the gate's exact flags and verified `PASS` locally; (8) C16/C17 reversibility vs forward-only VII downgrades — implemented exact reverse downgrades for both VII revisions and proved a 202609071200 round trip; (9) `Validate Migrations` vs `DROP TABLE` in the new downgrade — inline `CI:DESTRUCTIVE_OK` marker; (10) B2.5-P1..P12 scope/drift gates vs the five new VII paths — P1 allowlist entries, P2 serializer/scope allowlists, P5 unit-double tolerance in `apply_tenant_policy`; (11) P1 contract closure vs open `SimulationRead` — declared the full closed read shape in both contract authorities; (12) route fidelity vs undeclared simulation operations — declared both operations with matching operationIds in `trust-api.openapi.yaml`; (13) M3 governance vs the unregistered manifest-regenerator script — registry + subsumption utility entries; (14) B0/B1 bare-DB migration vs unconditional VII GRANTs/CREATE ROLE — role-tolerant grants, role provisioning left to the boundary script; (15) R3/B1.2-P9 readiness vs the exact-catalog manifest on minimally-provisioned topologies — readiness enforces the manifest only where the VII role graph is present (P14/C19/production) and abstains elsewhere with an explicit marker; (16) forensics INDEX vs the new VII report — indexed; (17) CI queue latency and one B0.4 single-timeout storm flake plus one C7 invalidation-timing flake — never rerun-away; both passed on subsequent runs. No product red was ever rerun-away: every failure above was dispositioned to a cause before proceeding.

---

## 4. Remaining debt and explicit limitations

- **Full fresh C19 with real Bayesian fit was not re-driven locally in this session** (9-service topology build + ~5-minute fit + governed run). It is inherited from Agent-1's executed positive/negative legs and is re-adjudicated by required CI on the exact candidate tree (production images built from candidate bytes, fresh PostgreSQL, `alembic upgrade head` to `202609072001`). If CI's C19 lane reddens for a causal reason, the candidate is not complete per §33.
- **Partial-transaction taxonomy** (§19) is evidenced at the refusal level (refused selections leave `(requests,results,proposals,witnesses)` census unchanged; durable success census is `(1,1,1,1)`/`(2,2,2,2)`) but a per-stage crash-injection matrix (after mint / after request / before consequence / after consequence / before proposal / before HTTP response) was not re-driven locally; the persistence layer's atomicity is inherited from Corrective V/VI proofs.
- **`X-Idempotency-Key` composition**: the P14 business idempotency key is `request_ref` (duplicate → refused; new ref → distinct request, proved). Transport retries must reuse the same `request_ref`; a retry with a fresh ref is a new business request by contract. No outer-header deduplication was added; documented, not built.
- **B2.4 runtime metrics** (chains/draws/tuning/R-hat/ESS/divergences) are inherited from Agent-1's real-fit run under governed worker limits; this session changed no statistical resources and the fit is re-driven by CI/C19, not locally.
- Non-blocking hardening debt inherited: DB secret comparison (`<>` vs app `compare_digest`, decision-equivalent); witness retention policy; orphan migration dir outside `version_locations`.

---

## 5. Completion equation (VII-3 §33) — engineering claim

All of the following are evidenced above on the exact candidate `51a6e28f`/`1464c72a`/head `202609072001`:

Agent-1/2 working state preserved · migration head constructed linearly · final Trust identity maps to the exact signer artifact · shared logical `envelope_id` safely disambiguated by the final hash · NULL final-source lookup fails closed · source and current policy both conserve authority (dual-conjunct) · `read_only` cannot be retrospectively upgraded · publisher least-privilege and append-only · possession credential-bound and single-use · freshness is real wall-clock · server derives financial evidence · snapshot drift refused · mixed currencies cannot aggregate · duplicate/retry explicit · partial states honest at the refusal census · deterministic allocation exact · B2.7 coherent on final identity and semantically bounded · B2.4 non-causal · zero external execution · physical schema matches reviewed manifest · A/B equivalent · legacy invariants reconciled and green · 11 successor tests green · active falsifiers non-vacuous · candidate committed and pushed · protected merge queue / ALLGREEN governance · landed main CI green *(pending queue; verified post-landing per §35)* · no post-landing drift *(verified post-landing)*.

Engineering therefore claims `CORRECTIVE VII = CANDIDATE COMPLETE`. It does not declare `B2.5-P14 = COMPLETE`; that remains the independent audit determination on the landed tree.

---

## 6. Landing governance (to be completed by the queue)

- Candidate: `51a6e28f78926f211d081c5c2231c03d5c523817` / tree `1464c72a5b205dac9f78997bfa86a681b0cd6132` / head `202609072001`
- PR: `#722` (`codex/p14-corrective-vii2` → `main`)
- Merge-group SHA/tree: *(queue evidence)*
- Landed `main` SHA/tree/head: *(must match candidate tree; verified via `git ls-remote` + fresh clone)*
- Required-context census: *(80+ required contexts incl. new VII-2 step; all green, zero missing — recorded from the landed SHA's check runs)*
- Post-landing verification on exact `main`: required contexts all successful · production image built from landed tree · migration head `202609072001` · manifest included · minimum load-bearing proof re-run (VII-2 11 + Gate-0 4) green.
